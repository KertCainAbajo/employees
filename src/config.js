// Change this value if your Freehostia API uses a different URL.
// The host currently responds over HTTP; its HTTPS endpoint is not responding.
// Enable HTTPS on the host before publishing the app outside classroom testing.
export const API_URL = 'http://kertabajo.mooo.com/employees';

// Leave empty while your PHP API authentication is disabled.
// Never commit a real secret token to a public repository.
export const API_TOKEN = '';
